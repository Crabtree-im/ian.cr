# Database — individual vars (used by db.py)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=hungergames

# Comma-separated admin API keys for protected endpoints
ADMIN_API_KEYS=change-me-before-use

# Server
PORT=8000
