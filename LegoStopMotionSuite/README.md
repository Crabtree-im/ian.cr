# Lego Stop Motion Suite

This folder contains a production-oriented scaffold for a two-part stop-motion system:

1. `iOSCaptureApp` - iPhone frame capture app
2. `macOSEditorApp` - macOS timeline editor and exporter
3. `Shared` - shared Swift package used by both apps

See `CHANGELOG.md` for milestone-by-milestone progress.

## Transfer Recommendation

Primary transfer method: **MultipeerConnectivity**.

Why this is the best fit for this project:

- Offline local transfer over Wi-Fi/Bluetooth.
- Predictable app-to-app flow for repeated capture sessions.
- No cloud account or external infrastructure required.
- Better automation and less user friction than AirDrop-only workflows.

Fallback options:

- AirDrop: easy backup path, but less automatable.
- iCloud container: useful for remote/asynchronous workflows, but slower and less deterministic on set.

## Integration Notes

Use the included XcodeGen configuration to generate a runnable multi-target project:

1. Install XcodeGen: `brew install xcodegen`
2. From this folder run: `./bootstrap_xcode.sh`
3. Open `XcodeGen/LegoStopMotionSuite.xcodeproj`
4. Set your development team for both targets.

Included permissions and capabilities are configured in:

- `XcodeGen/Plists/CaptureApp-Info.plist`
- `XcodeGen/Plists/EditorApp-Info.plist`
- `XcodeGen/Entitlements/EditorApp.entitlements`

## Run On Personal iPhone

If CaptureApp runs in simulator but not on your personal device, use this checklist:

1. Open Xcode and add your Apple ID:
	- `Xcode -> Settings -> Accounts`
2. Connect iPhone with USB and trust both devices if prompted.
3. Enable Developer Mode on iPhone:
	- `Settings -> Privacy & Security -> Developer Mode`
4. Open `XcodeGen/LegoStopMotionSuite.xcodeproj`.
5. Select target `CaptureApp`:
	- In Signing, choose your Personal Team.
	- Keep "Automatically manage signing" enabled.
6. Ensure the bundle identifier is unique for your account.
	- If needed, update it in `XcodeGen/project.yml` and re-run `./bootstrap_xcode.sh`.
7. Select your physical iPhone as destination and press Run.

If camera capture still fails on device:

1. Open iPhone Settings and verify camera permission for CaptureApp is enabled.
2. Launch CaptureApp and wait until the capture button changes from "Starting Camera..." to "Capture".
3. If permission was previously denied, re-enable it and relaunch the app.

## macOS Import Behavior

EditorApp import supports two paths from the same button:

1. Select exactly one `.zip` batch generated by CaptureApp, or
2. Select one or more image files directly (`.jpg`, `.jpeg`, `.png`, `.heic`, etc.).

The picker rejects mixed selections (zip + images together) to avoid ambiguous import behavior.

## Additional Implemented Features

- iOS onion-skin overlay with adjustable opacity during capture.
- iOS AirDrop fallback via `ShareLink` for any prepared batch zip.
- iOS `New Take` action to reset a capture batch quickly between scenes.
- macOS manual zip import (`Import Zip...`) for AirDrop-received files.
- macOS receiver start/stop control to manage transfer session lifecycle.

## Data Flow

1. iOS captures JPEG frames into app sandbox.
2. iOS writes `manifest.json` and zips batch folder.
3. iOS sends zip via MultipeerConnectivity to macOS app.
4. macOS imports zip into project folder.
5. macOS builds timeline at 12-15 FPS with equal frame durations.
6. macOS exports animation to `.mov`.

## Current Status (May 23, 2026)

Latest local commit on `feature/lego-stop-motion-app`:

- `29cad57` - "Lego stop motion building"

Working tree state:

- Clean (`git status --short` shows no output)

What this checkpoint includes:

- Xcode project generation flow validated with `bootstrap_xcode.sh`.
- `EditorAppTests` target plist/config fixed for testability.
- iOS CaptureApp compile blockers fixed:
  - iOS-compatible archive handling in `Shared`.
  - transfer service compile fixes for iOS.
- UX polish updates:
  - iOS `New Take`, upload gating, retry status clarity.
  - macOS receiver start/stop lifecycle controls.
  - macOS safer playback control state.
- macOS tests passing for `EditorApp` scheme.

Open blocker before real on-device run:

- No local code-signing identities are installed yet (`security find-identity -v -p codesigning` returns `0 valid identities found`).

## How To See What Has Been Done

Run these commands from `LegoStopMotionSuite`:

1. Current branch + clean/dirty state:

	```bash
	git status -sb
	```

2. Most recent commits:

	```bash
	git log --oneline -n 10
	```

3. Exactly what changed in the latest checkpoint:

	```bash
	git show --name-status --stat -n 1
	```

4. See full patch for the latest checkpoint:

	```bash
	git show -n 1
	```

5. Confirm iOS simulator runtime is installed:

	```bash
	xcrun simctl list runtimes
	xcrun simctl list devices available
	```

6. Verify build/test health:

	```bash
	xcodebuild build -project XcodeGen/LegoStopMotionSuite.xcodeproj -scheme CaptureApp -destination 'generic/platform=iOS Simulator'
	xcodebuild test -project XcodeGen/LegoStopMotionSuite.xcodeproj -scheme EditorApp -destination 'platform=macOS,arch=arm64'
	```
